<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_selectTienda"
        android:icon="@drawable/ic_swap_horizontal_bold"
        android:title="@string/cambiar_tienda"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_comprar"
        android:icon="@drawable/ic_comprado"
        android:title="@string/comprar"
        app:showAsAction="always"/>
    <item
        android:id="@+id/action_edit"
        android:icon="@drawable/ic_pencil"
        android:title="@string/editar"
        app:showAsAction="always"/>

</menu>